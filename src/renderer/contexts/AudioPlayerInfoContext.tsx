import { createContext } from 'react';

// export interface AudioPlayerInfoContextType {
//   songPosition: number;
// }

// eslint-disable-next-line import/prefer-default-export
export const AudioPlayerInfoContext = createContext({} as Player);
